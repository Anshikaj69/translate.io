const inputChange = (e) => { setText(e.target.value) }


function handleSubmit() {

}

// UI Controller

const UIctrl = (function () {
    const UIselectors = {
        inputLink: '#input_link',
        shortnbtn: '#submit_btn',
        errormsg: '#error_msg',
        copyLink: '#copy_btn',
        linksContainer: '.links_container'
    }

});